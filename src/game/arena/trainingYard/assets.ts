/**
 * Training Yard is currently built from procedural Babylon primitives.
 * Future GLB, texture, or audio dependencies for this map belong here so
 * they remain within the Training Yard lazy-loaded module.
 */
export const TRAINING_YARD_ASSETS = {
  models: [],
  textures: [],
} as const;
